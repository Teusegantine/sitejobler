<IfModule mod_rewrite.c>
RewriteEngine On

# Forçar HTTPS sem alterar o domínio (evita loops com a Hostinger)
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
</IfModule>
